c
# TODO - Fix ifelse8.c

- [x] Leer el archivo `ifelse8.c` y ubicar los errores de compilación/lógica.

- [x] Corregir el tipo de `nombre` (de `char` a `char nombre[50]`).

- [x] Corregir el `scanf` para `nombre` con tamaño máximo (ej: `%49s`).

- [x] Agregar `break;` en `case 11` y también un `break;` en `default`.

- [x] Verificar que el flujo del `switch` no haga “fall-through”.

- [x] Ejecutar/compilar para confirmar que ya no sale error.


